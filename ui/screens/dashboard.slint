import { Theme, AuraButton, AuraInput, AuraCard } from "../aura-ui/aura.slint";

export component Dashboard inherits HorizontalLayout {
        AuraCard {
        title: "My Card";

        Text {
            text: "Card content goes here";
        }
    }
}
